#ifndef _SHM_QUEUE_H#define _SHM_QUEUE_H// 共享内存循环队列typedef struct _ShmQueue ShmQueue;// 创建基于共享内存的循环队列ShmQueue *shmq_create(const char *file_name,const int file_size);// 销毁循环队列int shmq_destroy(ShmQueue *shmque);// 循环队列长度int shmq_size(ShmQueue *shmque);// 循环队列读数据int shmq_read(ShmQueue *shmque,char **ptr,short *len);//循环队列读所有数据// sunwakeint shmq_read_all(ShmQueue *shmque,char **ptr,int *len);// 循环队列写数据int shmq_write(ShmQueue *shmque,const char *ptr,const short len);#endif  // end shmque.h
